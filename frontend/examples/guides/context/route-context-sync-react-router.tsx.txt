import { useLocation } from 'react-router-dom';

function RouteContextSync() {
  const { pillar } = usePillar();
  const location = useLocation();

  useEffect(() => {
    pillar?.setContext({ currentPage: location.pathname });
  }, [pillar, location]);

  return null;
}
