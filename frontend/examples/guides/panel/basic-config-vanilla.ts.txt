import { Pillar } from '@pillar-ai/sdk';

await Pillar.init({
  agentSlug: 'your-agent-slug',
  config: {
    panel: {
      enabled: true,
      position: 'right',
      mode: 'push',
      width: 380,
    },
  },
});
