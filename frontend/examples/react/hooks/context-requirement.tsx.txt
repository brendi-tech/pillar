// ❌ Error: Hook used outside provider
function BadComponent() {
  const { isReady } = usePillar(); // Throws error
}

// ✅ Correct: Inside provider tree
function App() {
  return (
    <PillarProvider agentSlug="your-agent-slug">
      <GoodComponent />
    </PillarProvider>
  );
}

function GoodComponent() {
  const { isReady } = usePillar(); // Works!
}
