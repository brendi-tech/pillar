Pillar.init({
  productKey: 'your-product-key',
  publicKey: 'pk_live_xxx',
  
  panel: {
    position: 'right',
    mode: 'push',
    width: 400,
  },
  
  theme: {
    mode: 'auto',
    colors: {
      primary: '#2563eb',
    },
  },
});
