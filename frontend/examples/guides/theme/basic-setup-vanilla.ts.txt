import { Pillar } from '@pillar-ai/sdk';

await Pillar.init({
  agentSlug: 'your-agent-slug',
  config: {
    theme: {
      mode: 'auto',
      colors: {
        primary: '#2563eb',
      },
    },
  },
});
