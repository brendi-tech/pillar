import { PillarProvider } from '@pillar-ai/react';

function App() {
  return (
    <PillarProvider
      productKey="your-product-key"
      config={{
        textSelection: {
          enabled: true,
          label: 'Ask co-pilot',
        },
      }}
    >
      <YourApp />
    </PillarProvider>
  );
}
