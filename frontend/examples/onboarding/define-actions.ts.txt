// hooks/usePillarActions.ts
import { usePillarAction } from '@pillar-ai/react';
import { useRouter } from 'next/navigation';

export function usePillarActions() {
  const router = useRouter();

  usePillarAction({
    name: 'open_settings',
    type: 'navigate',
    description: 'Navigate to the settings page',
    examples: ['open settings', 'go to settings', 'settings'],
    autoRun: true,
    execute: () => router.push('/settings'),
  });

  usePillarAction({
    name: 'contact_support',
    type: 'open_modal',
    description: 'Open contact support form',
    examples: ['contact support', 'talk to someone', 'get help'],
    execute: () => openSupportModal(),
  });
}