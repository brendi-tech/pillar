import { PillarProvider } from '@pillar-ai/react';

function App() {
  return (
    <PillarProvider
      productKey="your-product-key"
      publicKey="pk_live_xxx"
      textSelection={{
        enabled: true,
        label: 'Ask co-pilot',
      }}
    >
      <YourApp />
    </PillarProvider>
  );
}
