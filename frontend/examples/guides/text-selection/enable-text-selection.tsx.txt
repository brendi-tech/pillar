import { PillarProvider } from '@pillar-ai/react';

function App() {
  return (
    <PillarProvider
      agentSlug="your-agent-slug"
      config={{
        textSelection: {
          enabled: true,
          label: 'Ask co-pilot',
        },
      }}
    >
      <YourApp />
    </PillarProvider>
  );
}
