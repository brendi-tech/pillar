<PillarProvider
  productKey="your-product-key"
  onTask={(task) => {
    switch (task.name) {
      case 'navigate':
        router.push(task.data.path);
        break;
      case 'open_settings':
        setShowSettings(true);
        break;
    }
  }}
>
