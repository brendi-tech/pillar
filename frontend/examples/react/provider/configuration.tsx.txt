<PillarProvider
  agentSlug="your-agent-slug"
  config={{
    // Forward user's auth token to API tool sources (optional)
    // userApiToken: accessToken,

    // Panel settings
    panel: {
      position: 'right',
      mode: 'push',
      width: 400,
    },
    
    // Theme
    theme: {
      mode: 'auto',
      colors: {
        primary: '#2563eb',
      },
    },
  }}
>
