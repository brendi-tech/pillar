usePillarAction({
  name: "my_action",
  description: "Example action showing return values",
  execute: async () => {
    // Success
    return { success: true };

    // Success with message
    return { success: true, message: "Invite sent!" };

    // Failure
    return { success: false, message: "Something went wrong" };
  },
});